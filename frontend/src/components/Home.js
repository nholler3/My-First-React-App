import React from 'react';

function Home(){
    return(
        //everything in return must be inside of a block
        //bootstrap code below, use for future changes
        //section is the block
        <section>
            <div>
                <h1>Welcome to my react app</h1>
                <p>This site was create using node.js and React</p>
            </div>
        </section>

    );
}

export default Home;